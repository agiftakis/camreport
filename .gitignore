# Frigate runtime data - ignore everything in config/ except config.yml
frigate/config/*
!frigate/config/config.yml
# secrets (camera passwords go in .env, referenced from config.yml as {FRIGATE_RTSP_PASSWORD})
.env
*.env
# local test images / footage - never commit media to the public repo
test.jpg
*.jpg
*.jpeg
*.png
*.mp4
*.webp
# editor / OS
.vscode/
Thumbs.db
desktop.ini
# Next.js app (added later)
node_modules/
.next/